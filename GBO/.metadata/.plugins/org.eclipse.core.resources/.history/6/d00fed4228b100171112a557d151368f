package gui.uebungen;

import javafx.application.Application;
import javafx.scene.Scene;
import javafx.scene.control.Label;
import javafx.scene.layout.StackPane;
import javafx.stage.Stage;

public class Main01 extends Application
{

    @Override
    public void start(Stage primaryStage) throws Exception
    {
        Label l = new Label("Hallo Welt");
        Label l2 = new Label("test");
        StackPane root = new StackPane();
        root.getChildren().add(l); // kommt container über getChildren zurück,
                                   // und da drauf wird add angewendet

        Scene scene = new Scene(root, 300, 300);
        primaryStage.setScene(scene);
        primaryStage.setTitle("Hell World");
        primaryStage.show();

    }

    public static void main(String[] args)
    {
        launch(args);
    }

}
